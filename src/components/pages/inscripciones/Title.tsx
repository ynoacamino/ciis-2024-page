import { Title } from '@/components/ui/title';

export default function AboutTitle() {
  return (
    <Title
      title="Incripciones"
      subtitle="El presente evento es totalmente gratuito, por lo que los participantes deberán inscribirse mediante un formulario."
    />
  );
}
